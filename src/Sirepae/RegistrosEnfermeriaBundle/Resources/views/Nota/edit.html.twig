{% extends 'SirepaeRegistrosEnfermeriaBundle:RegistroEnfermeria:index.html.twig' %}

{% block content -%}
    <div class="col-sm-12">
        <div class="pull-left">
            <h1 class="title">Observación de {{ entity.registroEnfermeria }}</h1>
        </div>
        <div class="pull-right">
            {{ form(delete_form) }}
        </div>
    </div>

    {{ form(edit_form) }}

    {% if re is defined and re is not null %}
        <a class="btn btn-default btn-lg pull-right" href="{{ path('registros_enfermeria_edit',{id: re.id}) }}">Volver</a>
    {% elseif is_granted('ROLE_USER') %}
        <a class="btn btn-default btn-lg pull-right" href="{{ path('nota') }}">Volver</a>
    {% endif %}
{% endblock %}
