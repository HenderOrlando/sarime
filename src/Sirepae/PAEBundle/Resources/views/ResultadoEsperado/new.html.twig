{% extends 'SirepaePAEBundle:PAE:index.html.twig' %}

{% block content -%}

    <h1 class="title">Nuevo Resultado Esperado</h1>

    {{ form(form) }}

    {% if noc is not null %}
        <a class="btn btn-default btn-lg pull-right" href="{{ path('noc_edit', {id: noc.id}) }}">Volver</a>
    {% else %}
        <a class="btn btn-default btn-lg pull-right" href="{{ path('resultado_esperado') }}">Volver</a>
    {% endif %}
{% endblock %}
