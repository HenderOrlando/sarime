{% extends 'SirepaePracticasBundle:Practica:index.html.twig' %}

{% block content -%}
    <div class="col-sm-12">
        <div class="pull-left">
            <h1 class="title">Sitio {{ entity.nombre }}</h1>
        </div>
        <div class="pull-right">
            {{ form(delete_form) }}
        </div>
    </div>

    {{ form(edit_form) }}

<div class="btn-group pull-right">
    <a class="btn btn-default btn-lg" href="{{ path('sitio') }}">Volver</a>
</div>
{% endblock %}
